apiVersion: v1
kind: Service
metadata:
  name: cluster-gateway
  namespace: banking-demo
  labels:
    app: cluster-gateway
spec:
  selector:
    app: cluster-gateway
  ports:
  - name: http
    port: 8080
    targetPort: 8080
