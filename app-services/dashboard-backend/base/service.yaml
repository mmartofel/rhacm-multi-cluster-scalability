apiVersion: v1
kind: Service
metadata:
  name: dashboard-backend
  namespace: banking-demo
  labels:
    app: dashboard-backend
spec:
  selector:
    app: dashboard-backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
