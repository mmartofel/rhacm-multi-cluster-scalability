apiVersion: v1
kind: Service
metadata:
  name: transaction-generator
  namespace: banking-demo
  labels:
    app: transaction-generator
spec:
  selector:
    app: transaction-generator
  ports:
  - name: http
    port: 8080
    targetPort: 8080
